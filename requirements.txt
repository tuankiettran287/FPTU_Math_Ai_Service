# Core deep learning / LLM runtime
torch>=2.3.0
transformers>=4.46.0
accelerate>=0.33.0
safetensors>=0.4.3
sentencepiece>=0.2.0
protobuf>=4.25.0

# Fine-tuning
datasets>=2.20.0
peft>=0.12.0
bitsandbytes>=0.43.0; platform_system != "Windows"

# Embeddings / vector search helpers
sentence-transformers>=3.0.0
scikit-learn>=1.4.0
scipy>=1.11.0
numpy>=1.26.0

# PostgreSQL + pgvector
psycopg[binary]>=3.2.0

# OCR for uploaded images
Pillow>=10.0.0
pytesseract>=0.3.13

# CLI / progress
tqdm>=4.66.0
