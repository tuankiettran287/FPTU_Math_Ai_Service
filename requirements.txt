# ── Core service (chạy được ở máy không GPU) ──────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.7.0,<3.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# DB: Postgres + pgvector
psycopg[binary]>=3.2.0,<4.0.0
pgvector>=0.3.0

# ── Chỉ cần ở nơi CHẠY MODEL (GPU pod) — KHÔNG cài trên máy dev không GPU ──────
# LLM serving khuyến nghị dùng vLLM (OpenAI-compatible) thay transformers thuần:
#   vllm>=0.6.0
# Embedding local (nếu không dùng endpoint remote):
#   sentence-transformers>=3.0.0
# Nền tảng cho transformers fallback / sentence-transformers:
#   torch>=2.3.0,<2.8.0
#   transformers>=4.46.0,<5.0.0
#   accelerate>=0.33.0,<2.0.0
#   safetensors>=0.4.3
#   sentencepiece>=0.2.0
#   protobuf>=4.25.0
